nextflow_pipeline {

    name "Test pre-demultiplexed barcode directory discovery"
    script "../main.nf"

    // Test Level & Component
    tag "pipeline"
    tag "integration"

    // Feature Area
    tag "barcode_discovery"

    // Execution Speed & Criticality
    tag "slow"
    tag "core"

    test("Should discover and process pre-demultiplexed barcode folders") {

        when {
            params {
                // Use barcode directory input
                barcode_input_dir = "$projectDir/tests/fixtures/barcode_structure"
                outdir = "$outputDir"

                // Provide required input parameter (empty samplesheet for barcode mode)
                input = false
                realtime_mode = false

                // Disable advanced features for focused testing
                skip_kraken2 = true
                blast_validation = false

                // Resource limits
                max_cpus = 2
                max_memory = '4.GB'
                max_time = '1.h'
            }
        }

        then {
            assert workflow.success
            assert path("${outputDir}/pipeline_info").exists()
            assert workflow.trace.tasks().size() > 0

            // Check QC ran for multiple barcode samples (barcode01, barcode02, unclassified)
            def qc_tasks = workflow.trace.tasks().findAll { it.name.contains('CHOPPER') || it.name.contains('FASTP') || it.name.contains('FILTLONG') }
            assert qc_tasks.size() >= 2  // At least 2 barcodes discovered and processed

            // Check output structure - default QC tool is CHOPPER
            assert path("${outputDir}/chopper").exists() || path("${outputDir}/fastp").exists()
            assert path("${outputDir}/multiqc").exists()
        }
    }

    test("Should handle empty barcode directories gracefully") {

        when {
            params {
                // Use a non-existent directory to test graceful handling
                input = false
                barcode_input_dir = "$projectDir/tests/fixtures/empty_barcode_structure"
                outdir = "$outputDir"

                skip_kraken2 = true
                blast_validation = false
                // Resource limits
                max_cpus = 2
                max_memory = '4.GB'
                max_time = '1.h'
            }
        }

        then {
            // An empty input directory is operator error, not a pipeline
            // fault: INPUT_SCANNER warns and emits an empty sample channel
            // (subworkflows/local/input_scanner/main.nf:98), so the run
            // completes without scheduling per-sample work. MULTIQC still
            // runs once over the empty set, so the task count is 1 rather
            // than 0 -- assert the absence of per-sample work directly
            // instead of a bare total.
            //
            // The previous assertion here was `workflow.failed ||
            // workflow.success`, which is true of every terminated workflow
            // and so passed for any behaviour, including a stack trace.
            assert workflow.success

            def perSample = workflow.trace.tasks().findAll { task ->
                ['CHOPPER', 'FASTP', 'FILTLONG', 'KRAKEN2', 'SEQKIT'].any {
                    task.name.contains(it)
                }
            }
            assert perSample.isEmpty() : "empty input scheduled per-sample work: ${perSample*.name}"
        }
    }
}
