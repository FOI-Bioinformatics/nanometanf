config {

    testsDir "."
    workDir System.getenv("NFT_WORKDIR") ?: ".nf-test"
    configFile "tests/nextflow.config"
    // Note: tests/nextflow.config already enables docker and sets resource limits
    // Using empty profile allows the test config to control all settings
    profile ""

}
