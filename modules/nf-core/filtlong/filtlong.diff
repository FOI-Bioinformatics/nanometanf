Changes in component 'nf-core/filtlong'
'modules/nf-core/filtlong/environment.yml' is unchanged
'modules/nf-core/filtlong/meta.yml' is unchanged
Changes in 'filtlong/main.nf':
--- modules/nf-core/filtlong/main.nf
+++ modules/nf-core/filtlong/main.nf
@@ -3,7 +3,7 @@
     label 'process_low'
 
     conda "${moduleDir}/environment.yml"
-    container "${ workflow.containerEngine == 'singularity' && !task.ext.singularity_pull_docker_container ?
+    container "${ workflow.containerEngine in ['singularity', 'apptainer'] && !task.ext.singularity_pull_docker_container ?
         'https://depot.galaxyproject.org/singularity/filtlong:0.2.1--h9a82719_0' :
         'biocontainers/filtlong:0.2.1--h9a82719_0' }"
 
@@ -23,13 +23,25 @@
     def prefix = task.ext.prefix ?: "${meta.id}"
     def short_reads = !shortreads ? "" : meta.single_end ? "-1 $shortreads" : "-1 ${shortreads[0]} -2 ${shortreads[1]}"
     if ("$longreads" == "${prefix}.fastq.gz") error "Longread FASTQ input and output names are the same, set prefix in module configuration to disambiguate!"
+    // filtlong takes exactly one positional long-read file. A multi-file
+    // sample (every barcode directory of a MinKNOW run) is concatenated
+    // first; gzip members concatenate as a valid stream. Without this the
+    // extra files are passed as positionals and filtlong exits 1
+    // ("passed in argument, but no positional arguments were ready to
+    // receive it"): nanometa_live audit round 5, 2026-09-03, P1.
+    def longread_list = longreads instanceof List ? longreads : [longreads]
+    def concat_input = longread_list.size() > 1
+    def longread_input = concat_input ? "${prefix}.concat_input.fq.gz" : "${longreads}"
+    def concat_cmd = concat_input ? "cat ${longread_list.join(' ')} > ${longread_input}" : "true"
     """
+    $concat_cmd
     filtlong \\
         $short_reads \\
         $args \\
-        $longreads \\
+        $longread_input \\
         2>| >(tee ${prefix}.log >&2) \\
         | gzip -n > ${prefix}.fastq.gz
+    rm -f ${prefix}.concat_input.fq.gz
 
     cat <<-END_VERSIONS > versions.yml
     "${task.process}":

Changes in 'filtlong/tests/main.nf.test.snap':
--- modules/nf-core/filtlong/tests/main.nf.test.snap
+++ modules/nf-core/filtlong/tests/main.nf.test.snap
@@ -7,18 +7,18 @@
                         "id": "test",
                         "single_end": false
                     },
-                    "test_lr.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
+                    "test.filtlong.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
                 ]
             ],
             [
                 "versions.yml:md5,af5988f30157282acdb0ac50ebb4c8cc"
             ]
         ],
+        "timestamp": "2026-04-27T19:48:32.716663",
         "meta": {
-            "nf-test": "0.8.4",
-            "nextflow": "24.04.3"
-        },
-        "timestamp": "2024-08-06T10:51:29.197603"
+            "nf-test": "0.9.5",
+            "nextflow": "25.04.7"
+        }
     },
     "sarscov2 nanopore [fastq] + Illumina paired-end [fastq]": {
         "content": [
@@ -28,18 +28,18 @@
                         "id": "test",
                         "single_end": false
                     },
-                    "test_lr.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
+                    "test.filtlong.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
                 ]
             ],
             [
                 "versions.yml:md5,af5988f30157282acdb0ac50ebb4c8cc"
             ]
         ],
+        "timestamp": "2026-04-27T19:49:03.350674",
         "meta": {
-            "nf-test": "0.8.4",
-            "nextflow": "24.04.3"
-        },
-        "timestamp": "2024-08-06T10:51:39.68464"
+            "nf-test": "0.9.5",
+            "nextflow": "25.04.7"
+        }
     },
     "sarscov2 nanopore [fastq] + Illumina single-end [fastq]": {
         "content": [
@@ -49,17 +49,17 @@
                         "id": "test",
                         "single_end": true
                     },
-                    "test_lr.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
+                    "test.filtlong.fastq.gz:md5,7567d853ada6ac142332619d0b541d76"
                 ]
             ],
             [
                 "versions.yml:md5,af5988f30157282acdb0ac50ebb4c8cc"
             ]
         ],
+        "timestamp": "2026-04-27T19:48:47.936054",
         "meta": {
-            "nf-test": "0.8.4",
-            "nextflow": "24.04.3"
-        },
-        "timestamp": "2024-08-06T10:51:34.404022"
+            "nf-test": "0.9.5",
+            "nextflow": "25.04.7"
+        }
     }
 }
Changes in 'filtlong/tests/nextflow.config':
--- modules/nf-core/filtlong/tests/nextflow.config
+++ modules/nf-core/filtlong/tests/nextflow.config
@@ -1,4 +1,11 @@
+// The nanometanf main config (loaded by nf-test alongside this file) sets a
+// withName: 'FILTLONG' selector in conf/modules.config that overrides any
+// blanket process { ext.args / ext.prefix }. Match that selector strength here
+// so the module test runs in isolation with deterministic args and prefix,
+// independent of pipeline-level qc profile defaults.
 process {
-    ext.args = "--min_length 10"
-    ext.prefix = "test_lr"
+    withName: 'FILTLONG' {
+        ext.args = "--min_length 10"
+        ext.prefix = { "${meta.id}.filtlong" }
+    }
 }

Changes in 'filtlong/tests/main.nf.test':
--- modules/nf-core/filtlong/tests/main.nf.test
+++ modules/nf-core/filtlong/tests/main.nf.test
@@ -3,7 +3,11 @@
     name "Test Process FILTLONG"
     script "../main.nf"
     process "FILTLONG"
+
+    options "-stub"
+    tag "stub"
     config "./nextflow.config"
+    tag "fast"
     tag "filtlong"
     tag "modules"
     tag "modules_nfcore"

************************************************************
