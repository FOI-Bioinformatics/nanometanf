nextflow_workflow {

    name "Test REALTIME_MONITORING"
    script "../main.nf"
    workflow "REALTIME_MONITORING"

    // Test Level & Component
    tag "subworkflow"
    tag "realtime_monitoring"

    // Feature Area
    tag "realtime"

    // Execution Speed & Criticality
    tag "medium"
    tag "core"

    test("Should handle disabled real-time mode gracefully") {

        when {
            workflow {
                """
                input[0] = '$outputDir/test_watch_dir'     // watch_dir
                input[1] = '**/*.fastq{,.gz}'              // file_pattern
                input[2] = 5                               // batch_size
                input[3] = '2.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = null
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should validate parameters for real-time FASTQ monitoring") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/fastq'    // watch_dir
                input[1] = '**/*.fastq{,.gz}'              // file_pattern
                input[2] = 3                               // batch_size
                input[3] = '1.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = true
                max_files = "5"
                max_cpus = 2
                max_memory = '4.GB'
                max_time = '2.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()
        }
    }

    test("Should handle different file patterns correctly") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/mixed_formats'   // watch_dir
                input[1] = '*.fastq'                       // file_pattern - only .fastq extension
                input[2] = 2                               // batch_size
                input[3] = '30.sec'                        // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle different batch sizes correctly") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/batch_fastq'     // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 1                               // batch_size - single file batches
                input[3] = '10.sec'                        // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle large batch sizes") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/large_batch_fastq'    // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 25                              // batch_size - large batches
                input[3] = '5.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "100"
                max_cpus = 4
                max_memory = '8.GB'
                max_time = '5.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle different batch intervals") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/batch_fastq'  // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 3                               // batch_size
                input[3] = '30.sec'                        // batch_interval - short interval
                """
            }

            params {
                realtime_mode = false
                max_files = "1"
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle nested directory structures") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/nested'        // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern - recursive search
                input[2] = 4                               // batch_size
                input[3] = '2.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "2"
                max_cpus = 2
                max_memory = '4.GB'
                max_time = '2.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle empty watch directories") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/empty'    // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 5                               // batch_size
                input[3] = '1.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "0"
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle different FASTQ file formats") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/mixed_formats'    // watch_dir
                input[1] = '**/*.{fastq,fq}{,.gz}'         // file_pattern - multiple formats
                input[2] = 2                               // batch_size
                input[3] = '1.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "1"
                max_cpus = 2
                max_memory = '4.GB'
                max_time = '2.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should validate resource requirements for different scenarios") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/large_batch_fastq'  // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 50                              // batch_size - large batches
                input[3] = '10.min'                        // batch_interval - long interval
                """
            }

            params {
                realtime_mode = false
                max_files = "20"
                max_cpus = 8
                max_memory = '16.GB'
                max_time = '10.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle files with special characters in names") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/fastq'  // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 2                               // batch_size
                input[3] = '1.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "1"
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should handle very long file paths") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/fastq'     // watch_dir
                input[1] = '**/*.fastq'                    // file_pattern
                input[2] = 1                               // batch_size
                input[3] = '1.min'                         // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = 5
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }

    test("Should validate file pattern matching precision") {

        when {
            workflow {
                """
                input[0] = '$projectDir/tests/fixtures/watch_dirs/pattern_precision'  // watch_dir
                input[1] = '*.{fastq,fq}'                      // file_pattern - precise matching
                input[2] = 5                                   // batch_size
                input[3] = '1.min'                             // batch_interval
                """
            }

            params {
                realtime_mode = false
                max_files = "1"
                max_cpus = 1
                max_memory = '2.GB'
                max_time = '1.min'
            }
        }

        then {
            assert workflow.success
            assert snapshot(workflow.out.versions).match()

            if (workflow.out.samples) {
                def samples = workflow.out.samples.toList()
                assert samples.size() == 0
            }
        }
    }
}
